<h1>Task list</h1>

<% @tasks.each do |task| %>
<%= task.title %><br><%= link_to "Delete", task_path(task), method: :delete %>
<%= link_to "Edit", tasks_edit_path(task)%><br>
<% end %>

<%= link_to "Add a task", tasks_new_path %>
